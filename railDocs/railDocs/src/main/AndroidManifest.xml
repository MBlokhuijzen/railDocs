<?xml version="1.0" encoding="utf-8"?>
<manifest
    package="nl.loxia.raildocs.raildocs"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity_"
            android:label="@string/title_activity_login"
            android:windowSoftInputMode="adjustResize|stateVisible">
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".PostListActivity_"
            android:label="@string/title_activity_post_list"></activity>
        <activity
            android:name=".DossierListActivity_"
            android:label="@string/title_activity_dossier_list"></activity>
        <activity
            android:name=".DocumentListActivity_"
            android:label="@string/title_activity_document_list"></activity>
        <activity
            android:name=".BladenListActivity_"
            android:label="@string/title_activity_bladen_list"></activity>
    </application>

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />

</manifest>
